# .env.local

# Postgres connection (Neon or Supabase)
DATABASE_URL="postgresql://user:password@host:5432/scalecards?sslmode=require"

# Public base URL (for OG images + share links)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Inngest (optional for local dev — uses dev server)
INNGEST_EVENT_KEY=""
INNGEST_SIGNING_KEY=""

# Finnhub (optional — enables live market caps for Trillion Dollar Club card)
# Sign up free at https://finnhub.io/ — 60 calls/min free tier
FINNHUB_API_KEY=""

# Cron/admin secret — protects /api/refresh and /api/watchdog in production
# Generate with: openssl rand -hex 32
CRON_SECRET=""

